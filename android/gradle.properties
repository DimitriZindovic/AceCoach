org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template (default: false).
# Set to true: AGP 9 provides built-in Kotlin support, so neither the app nor the
# Firebase plugins need to apply the Kotlin Gradle Plugin (KGP) anymore.
android.builtInKotlin=true
